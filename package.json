{
  "name": "terpmeet",
  "version": "1.0.0",
  "description": "TerpMeet - Student Event Management Platform",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "npm run build && npm start"
  },
  "keywords": ["events", "students", "mongodb", "express"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
